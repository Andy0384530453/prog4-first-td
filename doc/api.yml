openapi: 3.0.3
info:
  title: BookStore API
  description: API for bookstore management featuring OpenLibrary integration for automated book data retrieval.
  version: 1.0.0
servers:
  - url: http://localhost:8080/api
    description: Development server

tags:
  - name: Books
    description: Book inventory and analytics
  - name: Sales
    description: Customer purchases and billing
  - name: Locations
    description: Book rentals and return tracking
  - name: Customers
    description: Customer profile management

paths:
  /books:
    get:
      summary: List all books
      tags:
        - Books
      description: Returns a complete list of books with optional filtering by genre.
      parameters:
        - name: genre
          in: query
          description: Filter books by genre
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of books retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BookDetail'
        '400':
          description: Bad request
    post:
      summary: Add a new book
      tags:
        - Books
      description: Receives an ISBN and prices, fetches missing data from OpenLibrary, and creates a database record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BookInput'
      responses:
        '201':
          description: Book created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BookDetail'
        '400':
          description: Invalid data or ISBN not found on OpenLibrary

  /books/top-profit:
    get:
      summary: List most profitable books
      tags:
        - Books
      description: Returns books sorted by total profit descending, calculated as (selling_price - purchase_price) * quantity_sold.
      responses:
        '200':
          description: Ranking of the most profitable books
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BookProfitDetail'

  /books/low-stock:
    get:
      summary: List low stock books
      tags:
        - Books
      description: Returns books with a current stock level less than or equal to 3.
      responses:
        '200':
          description: List of books requiring replenishment
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BookDetail'

  /sales:
    post:
      summary: Register a sale
      tags:
        - Sales
      description: Creates a new transaction record, links books with purchased quantities, and updates inventory stock.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaleInput'
      responses:
        '201':
          description: Sale registered successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SaleDetail'
        '400':
          description: Invalid sale data or insufficient stock

  /sales/{id}:
    get:
      summary: Get sale details
      tags:
        - Sales
      description: Returns full transaction details based on a unique sale identifier.
      parameters:
        - name: id
          in: path
          required: true
          description: Unique identifier of the sale
          schema:
            type: integer
      responses:
        '200':
          description: Sale details found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SaleDetail'
        '404':
          description: Sale not found

  /locations:
    post:
      summary: Register a book rental
      tags:
        - Locations
      description: Creates a rental record for associated books and decrements available inventory.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationInput'
      responses:
        '201':
          description: Rental registered successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationDetail'
        '400':
          description: Invalid rental data or insufficient stock

  /locations/{id}/return:
    put:
      summary: Process a rental return
      tags:
        - Locations
      description: Updates the actual return timestamp, changes rental status, and replenishes book inventory.
      parameters:
        - name: id
          in: path
          required: true
          description: Unique identifier of the rental
          schema:
            type: integer
      responses:
        '200':
          description: Return processed successfully and stock updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationDetail'
        '404':
          description: Rental record not found

  /customers:
    get:
      summary: List all customers
      tags:
        - Customers
      description: Returns a complete list of registered customer accounts.
      responses:
        '200':
          description: List of customers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CustomerDetail'
    post:
      summary: Create a customer profile
      tags:
        - Customers
      description: Adds a new customer profile to the database.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerInput'
      responses:
        '201':
          description: Customer profile created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerDetail'
        '400':
          description: Invalid customer data

components:
  schemas:
    Error:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          description: Explicit error message payload


    AuthorDetail:
      type: object
      required:
        - id_author
        - last_name
        - first_name
      properties:
        id_author:
          type: integer
        last_name:
          type: string
        first_name:
          type: string


    BookInput:
      type: object
      required:
        - isbn
        - purchase_price
        - selling_price
      properties:
        isbn:
          type: string
        purchase_price:
          type: number
          format: float
        selling_price:
          type: number
          format: float

    BookDetail:
      type: object
      required:
        - isbn
        - title
        - genre
        - purchase_price
        - selling_price
        - current_stock
      properties:
        isbn:
          type: string
        title:
          type: string
        genre:
          type: string
        publication_date:
          type: string
          format: date
        purchase_price:
          type: number
          format: float
        selling_price:
          type: number
          format: float
        current_stock:
          type: integer
        authors:
          type: array
          items:
            $ref: '#/components/schemas/AuthorDetail'

    BookProfitDetail:
      allOf:
        - $ref: '#/components/schemas/BookDetail'
        - type: object
          properties:
            total_profit:
              type: number
              format: float


    CustomerInput:
      type: object
      required:
        - first_name
        - last_name
      properties:
        first_name:
          type: string
        last_name:
          type: string
        phone_number:
          type: string
        email:
          type: string
          format: email

    CustomerDetail:
      type: object
      required:
        - id_customer
        - first_name
        - last_name
      properties:
        id_customer:
          type: integer
        first_name:
          type: string
        last_name:
          type: string
        phone_number:
          type: string
        email:
          type: string
          format: email


    BookQuantityReference:
      type: object
      required:
        - isbn
        - quantity
      properties:
        isbn:
          type: string
        quantity:
          type: integer


    SaleInput:
      type: object
      required:
        - customer_id
        - books
      properties:
        customer_id:
          type: integer
        books:
          type: array
          items:
            $ref: '#/components/schemas/BookQuantityReference'

    SaleDetail:
      type: object
      required:
        - id_sale
        - sale_date
        - payment_status
        - books
      properties:
        id_sale:
          type: integer
        sale_date:
          type: string
          format: date-time
        payment_status:
          type: string
          enum: [PENDING, PAID, CANCELLED]
        customer:
          $ref: '#/components/schemas/CustomerDetail'
        books:
          type: array
          items:
            $ref: '#/components/schemas/BookQuantityReference'


    LocationInput:
      type: object
      required:
        - customer_id
        - expected_return_date
        - books
      properties:
        customer_id:
          type: integer
        expected_return_date:
          type: string
          format: date
        books:
          type: array
          items:
            $ref: '#/components/schemas/BookQuantityReference'

    LocationDetail:
      type: object
      required:
        - id_location
        - location_date
        - expected_return_date
        - status
        - books
      properties:
        id_location:
          type: integer
        location_date:
          type: string
          format: date
        expected_return_date:
          type: string
          format: date
        actual_return_date:
          type: string
          format: date
          nullable: true
        status:
          type: string
          enum: [ACTIVE, RETURNED, LATE]
        customer:
          $ref: '#/components/schemas/CustomerDetail'
        books:
          type: array
          items:
            $ref: '#/components/schemas/BookQuantityReference'


    StockHistoryDetail:
      type: object
      required:
        - id_stock
        - isbn
        - movement_type
        - movement_date
        - quantity
      properties:
        id_stock:
          type: integer
        isbn:
          type: string
        movement_type:
          type: string
          enum: [IN, OUT]
        movement_date:
          type: string
          format: date-time
        quantity:
          type: integer